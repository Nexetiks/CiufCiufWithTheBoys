namespace Common.AIBase
{
    /// <summary>
    /// Enum with all possibly states of nodes.
    /// </summary>
    public enum NodeState
    {
        Running,
        Success,
        Failure
    }
}